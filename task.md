# 任务：字体子集化性能优化 + SSIM 质量保障（/loop 持续循环）

## 目标
1. **持续优化字体子集化性能**（速度、输出体积）
2. **SSIM ≥ 0.999 即可**：子集渲染与原始字体人眼无差别。SSIM 低于 0.999 = 一票否决，无论优化收益多大

## 硬性约束
- 每轮优化后必须通过 `pnpx tsx ./基准测试.test.ts`，任何一项不过都不能合入
- **禁止修改基准测试中的 full 生成方案**；基准结果异常 = 我的代码改动有问题，不要归咎于浏览器渲染等外部因素
- 优先正确性：每步改动用基准测试验证，小步快跑，不引入 bug

## 工作方式
- 直接开始做，不切计划模式、不询问、不中断，持续自主迭代
- 每个重大优化节点更新 `benchmark_results/OPTIMIZATION_LOG.md`，包含：
  - 使用的优化方法
  - 优化前后基准数据对比（avg/min/max 耗时、输出 bytes、ssim）
  - 效果评估与下一步方向

## 待探索方向
- **woff2 输出格式**：新增 woff2 支持（保留 ttf），woff2 设为默认
- **前端 SDK 扫描策略可配置化**：目前 SDK 用定时器扫描文本，增加配置项支持三种模式：
  - 定时扫描（默认，最省心）
  - 用户事件触发（由用户自己的事件驱动扫描）
  - 用户直接传文本（零扫描，首页 demo 场景响应更及时）

## 基线参考

=== 优化前 ===
  8个汉字: avg=23.6ms  min=18.4ms  max=37.2ms  输出=16,508 bytes  ssim=1.0000
  拉丁+数字: avg=16.4ms  min=13.7ms  max=18.2ms  输出=1,272 bytes  ssim=1.0000
  千字文前段: avg=59.4ms  min=47.3ms  max=76.5ms  输出=161,344 bytes  ssim=1.0000

=== 一晚优化后 ===
  8个汉字: avg=7.7ms   min=3.8ms  max=20.7ms  输出=16,572 bytes   ssim=1.0000
  拉丁+数字: avg=3.2ms  min=1.6ms  max=6.6ms   输出=1,272 bytes   ssim=1.0000
  千字文前段: avg=11.7ms min=6.8ms  max=21.7ms  输出=161,816 bytes  ssim=1.0000